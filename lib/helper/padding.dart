import 'package:flutter/material.dart';

// Padding untuk menu widget (hari ini, anak, & kegiatan)
EdgeInsetsGeometry paddingMenu = EdgeInsets.only(
  top: 40,
  bottom: 40,
  left: 50,
  right: 50,
);

// Padding untuk menu widget (hari ini, anak, & kegiatan)
EdgeInsetsGeometry paddingMobileMenu = EdgeInsets.only(
  top: 20,
  bottom: 20,
  left: 30,
  right: 30,
);
